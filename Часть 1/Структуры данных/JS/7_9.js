"use strict";

var endRange = +prompt('Введите конец диапазона:', '100');

var numRange = [];
var oddNumbers = []; //массив нечетных чисел
var erastofen [];

for (var i=2; i<(endRange+1); i++){
	numRange.push(i);
}

var p = numRange[0];
erastofen.push(p);
for (i=0; i<numRange.length; i++){
	if (){
		erastofen.push(numRange[i]);
	}
}

